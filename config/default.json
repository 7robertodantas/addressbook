{
  "port": 3000,
  "jwt": {
    "secretOrPrivateKey": "secret",
    "options": {
      "verify": {
        "algorithms": ["HS256"],
        "issuer": "addressbook-api",
        "maxAge": "1h"
      },
      "sign": {
        "algorithm": "HS256",
        "issuer": "addressbook-api",
        "expiresIn": "1h"
      }
    }
  },
  "bcrypt": {
    "saltRounds": 10
  }
}